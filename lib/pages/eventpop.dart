import 'package:flutter/material.dart';

class eventpop extends StatefulWidget {
  const eventpop({super.key});

  @override
  State<eventpop> createState() => _eventpopState();
}

class _eventpopState extends State<eventpop> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("dippppp"),
    );
  }
}
